# ── Telegram Bot Framework ────────────────────────────────────
aiogram==3.10.0
aiohttp==3.9.5
aiofiles==23.2.1

# ── Download Engine ───────────────────────────────────────────
yt-dlp>=2024.8.1

# ── Queue / Cache ─────────────────────────────────────────────
redis==5.0.7
aioredis==2.0.1

# ── AI APIs ───────────────────────────────────────────────────
openai>=1.40.0           # OpenAI + DeepSeek + OpenRouter (all OpenAI-compatible)
anthropic>=0.34.0        # Anthropic Claude
groq>=0.9.0              # Groq (Whisper transcription + Llama LLM)

# Google Gemini
google-generativeai>=0.7.0

# Hugging Face Inference API (used via httpx, no extra SDK needed)
# No additional package required — uses httpx below

# ── Translation ───────────────────────────────────────────────
httpx>=0.27.0            # DeepL + Hugging Face HTTP calls
deepl==1.18.0

# ── Audio Processing ──────────────────────────────────────────
pydub==0.25.1
demucs==4.0.1            # stem separation (vocals / music)

# ── Cloud Storage ─────────────────────────────────────────────
google-api-python-client==2.143.0
google-auth==2.34.0
google-auth-httplib2==0.2.0
dropbox==11.36.2
mega.py==1.0.8

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
